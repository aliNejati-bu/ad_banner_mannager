export class Banner {
    constructor(
        public _id: string,
        public name: string,
        public image: string,
        public url: string,
        public createdAt: Date,
        public updatedAt: Date,
    ) {
    }
}